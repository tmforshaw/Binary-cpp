#pragma once

// Sizes of binary numbers

const unsigned int defaultByteNum = 2;
const unsigned int intByteNum = 1;

// Format of outputted binary

const bool spaceNibbles = true; // Add spacing between nibbles
const bool padBytes = true; // Add spacing between bytes

/*	If Both spaceNibbles and padBytes are enabled there will be
	more padding around bytes to differentiate them. */